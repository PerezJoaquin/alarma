import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.pps.alarmaderobo',
  appName: 'Alarma de Robo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
